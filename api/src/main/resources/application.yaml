spring:
  application:
    name: base-service
  datasource:
    url: jdbc:postgresql://localhost:5432/novabank-card-service
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true